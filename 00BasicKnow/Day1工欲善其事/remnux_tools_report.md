# REMnux 工具发现报告
## ghidra
- 可执行路径：`/usr/bin/ghidra`
- 桌面启动器：`ghidra.desktop`
- 属于包：`ghidra: /usr/bin/ghidra`

## radare2
- 可执行路径：`/usr/bin/radare2`
- 属于包：`radare2: /usr/bin/radare2`

## cutter
- 可执行路径：`/usr/local/bin/cutter`
- 桌面启动器：`cutter.desktop`

## ida
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## binaryninja
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## gdb
- 可执行路径：`/usr/bin/gdb`
- 属于包：`gdb: /usr/bin/gdb`

## strace
- 可执行路径：`/usr/bin/strace`
- 属于包：`strace: /usr/bin/strace`

## ltrace
- 可执行路径：`/usr/bin/ltrace`
- 属于包：`ltrace: /usr/bin/ltrace`

## procmon
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## sysdig
- 可执行路径：`/usr/bin/sysdig`
- 属于包：`sysdig: /usr/bin/sysdig`

## yara
- 可执行路径：`/usr/bin/yara`
- 属于包：`yara: /usr/bin/yara`

## yarGen
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## capa
- 可执行路径：`/usr/local/bin/capa`

## floss
- 可执行路径：`/usr/bin/floss`
- 属于包：`flare-floss: /usr/bin/floss`

## pefile
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## lief
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## oletools
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## oledump.py
- 可执行路径：`/usr/bin/oledump.py`
- 属于包：`oledump: /usr/bin/oledump.py`

## olevba
- 可执行路径：`/usr/local/bin/olevba`

## pdfid
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## pdf-parser
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## apktool
- 可执行路径：`/usr/local/bin/apktool`

## jadx
- 可执行路径：`/usr/local/bin/jadx`

## mobSF
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## unpacme-cli
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## upx
- 可执行路径：`/usr/bin/upx`

## volatility
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## volatility3
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## rekall
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## autopsy
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## sleuthkit
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## inetsim
- 可执行路径：`/usr/bin/inetsim`
- 属于包：`inetsim: /usr/bin/inetsim`

## fakenet-ng
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## mitmproxy
- 可执行路径：`/usr/local/bin/mitmproxy`

## wireshark
- 可执行路径：`/usr/bin/wireshark`
- 桌面启动器：`org.wireshark.Wireshark.desktop`
- 属于包：`wireshark: /usr/bin/wireshark`

## tshark
- 可执行路径：`/usr/bin/tshark`
- 属于包：`tshark: /usr/bin/tshark`

## zeek
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## suricata
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## tcpflow
- 可执行路径：`/usr/bin/tcpflow`
- 属于包：`tcpflow: /usr/bin/tcpflow`

## ngrep
- 可执行路径：`/usr/bin/ngrep`
- 属于包：`ngrep: /usr/bin/ngrep`

## clamav
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## cyberchef
- 可执行路径：`/usr/local/bin/cyberchef`
- 桌面启动器：`cyberchef.desktop`

## remnux-cli
- 可执行路径：**未在 PATH 中找到**（可能为 GUI-only 或未安装）

## strings
- 可执行路径：`/usr/bin/strings`
- 属于包：`binutils: /usr/bin/strings`

## file
- 可执行路径：`/usr/bin/file`
- 属于包：`file: /usr/bin/file`

## binwalk
- 可执行路径：`/usr/bin/binwalk`
- 属于包：`binwalk: /usr/bin/binwalk`

## exiftool
- 可执行路径：`/usr/local/bin/exiftool`

## /opt 目录下可能的工具
- AndroidProjectCreator
- baksmali
- binee-files
- containerd
- dissect
- evilclippy-files
- extract-msg
- ghidra
- ilspycmd
- jd-gui-files
- magika
- malwoverview
- microsoft
- nth
- oledump-files
- portex
- procdot
- qiling
- runsc
- saltstack
- scdbg
- speakeasy
- thug
- vipermonkey

## /usr/share/applications 中的 REMnux 相关 .desktop（显示前40项）
cutter.desktop
ghidra.desktop
org.wireshark.Wireshark.desktop
